import styled from 'styled-components';

const Headsets = () => {
	return (
		<HeadsetsList>
			<h2>Headsets</h2>
		</HeadsetsList>
	);
};

export default Headsets;

const HeadsetsList = styled.div``;
